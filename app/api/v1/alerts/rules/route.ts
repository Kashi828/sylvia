import {NextRequest,NextResponse} from 'next/server';
import {createAlertRule,deleteAlertRule,listAlertRules,updateAlertRule} from '@/lib/alerts';
export async function GET(){return NextResponse.json({rules:listAlertRules()})}
export async function POST(req:NextRequest){try{const b=await req.json(); if(!b.name||!b.deviceId||!b.streamId||typeof b.threshold!=='number')return NextResponse.json({error:'name, deviceId, streamId and numeric threshold are required'},{status:400}); return NextResponse.json({rule:createAlertRule({name:b.name,deviceId:b.deviceId,streamId:b.streamId,operator:b.operator||'>',threshold:b.threshold,severity:b.severity||'warning',cooldownSeconds:Math.max(0,Number(b.cooldownSeconds??300)),enabled:b.enabled!==false,action:b.action||'none',webhookUrl:String(b.webhookUrl||'')})},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400})}}
export async function PATCH(req:NextRequest){try{const b=await req.json(); const {id,...patch}=b; if(!id)return NextResponse.json({error:'id is required'},{status:400}); const rule=updateAlertRule(id,patch); return rule?NextResponse.json({rule}):NextResponse.json({error:'Rule not found'},{status:404})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400})}}
export async function DELETE(req:NextRequest){const id=req.nextUrl.searchParams.get('id'); if(!id)return NextResponse.json({error:'id is required'},{status:400}); return NextResponse.json({ok:deleteAlertRule(id)})}
