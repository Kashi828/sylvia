// SYLVIA ESP32 starter sketch (transport-neutral template)
// Add WiFi + MQTT/HTTPS libraries in your deployment.
const char* SYLVIA_DEVICE_TOKEN = "YOUR_DEVICE_TOKEN";
const char* SYLVIA_DEVICE_ID = "YOUR_DEVICE_ID";

void setup() {
  // connect WiFi and then the configured SYLVIA transport
}

void loop() {
  // heartbeat();
  // publishDatastream("temperature", 25.4);
  // processCommands();
}
