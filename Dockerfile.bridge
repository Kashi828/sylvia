FROM node:22-alpine
WORKDIR /app
COPY workers/package.json ./package.json
RUN npm install --omit=dev
COPY workers/mqtt-bridge.mjs ./mqtt-bridge.mjs
RUN addgroup -S sylvia && adduser -S sylvia -G sylvia && chown -R sylvia:sylvia /app
USER sylvia
CMD ["node", "mqtt-bridge.mjs"]
